# The goal of this challenge was to simply find the factorial value of a supplied number.
# This is a simple solution using the math library.

# Import math library:
import math

def FirstFactorial(num):
  # Return the factorial for the supplied num parameter:
  return math.factorial(num)

# keep this function call here 
print(FirstFactorial(input()))
