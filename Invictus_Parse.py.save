#!/usr/bin/python

from lxml import html
import requests

#url = input("Enter URL for parsing: ")

page = requests.get('https://www.crossfitinvictus.com/wod/october-25-2018-performance/')
respData = html.fromstring(req,content)

data = respData.xpath('<p>(.*?)</p>')

print(data)
